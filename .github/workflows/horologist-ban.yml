name: Horologist ban

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ban:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan for Horologist
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ripgrep
          bash scripts/check-no-horologist.sh
